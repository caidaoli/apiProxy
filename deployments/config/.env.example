# API代理服务器环境变量配置示例
# 复制此文件为 .env 并修改相应值

# ========== 服务器配置 ==========
# 服务器监听端口
PORT=8000

# ========== Redis配置 ==========
# Redis连接URL (推荐格式)
# 格式: redis://[username]:password@host:port/db  (标准连接)
#      rediss://[username]:password@host:port/db (TLS加密连接)
# 示例:
#   - 有密码: redis://:mypassword@localhost:6379/0
#   - 无密码: redis://localhost:6379/0
#   - Docker: redis://:mypassword@redis:6379/0
#   - TLS加密: rediss://:mypassword@secure-redis.example.com:6380/0
API_PROXY_REDIS_URL=redis://:your_secure_redis_password@localhost:6379/0

# ========== 管理功能配置 ==========
# 管理令牌 (用于访问 /admin 管理界面和API)
# 请设置一个强随机字符串,例如: openssl rand -hex 32
ADMIN_TOKEN=your_secure_admin_token_here

# ========== 使用说明 ==========
# 1. 复制此文件: cp .env.example .env
# 2. 修改 API_PROXY_REDIS_URL 和 ADMIN_TOKEN 为安全的值
# 3. 启动Redis: docker run -d -p 6379:6379 redis:7-alpine --requirepass your_secure_redis_password
# 4. 初始化数据: API_PROXY_REDIS_URL=redis://:your_password@localhost:6379/0 go run scripts/init_redis.go
# 5. 启动服务: go run main.go stats.go redis.go admin.go
#
# Docker Compose使用:
# 1. 复制此文件: cp .env.example .env
# 2. 修改配置值
# 3. 启动服务: docker-compose up -d
